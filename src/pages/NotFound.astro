---
// 404 page for Astro - automatically served for non-existent routes
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>404 - Page Not Found | DJ Brian Rodrigues</title>
    <meta
      name="description"
      content="The page you're looking for doesn't exist. Return to DJ Brian Rodrigues's homepage."
    />
  </head>
  <body>
    <div
      class="min-h-screen flex items-center justify-center bg-gradient-to-br from-gray-900 via-slate-900 to-black"
    >
      <div class="text-center px-6 max-w-lg">
        <!-- 404 Number with gradient -->
        <h1
          class="text-8xl md:text-9xl font-bold mb-6 bg-gradient-to-r from-blue-400 via-purple-400 to-pink-400 bg-clip-text text-transparent"
        >
          404
        </h1>

        <!-- Error message -->
        <h2 class="text-2xl md:text-3xl font-semibold text-white mb-4">Oops! Page not found</h2>

        <p class="text-lg text-gray-300 mb-8 leading-relaxed">
          The page you're looking for seems to have disappeared into the mix. Let's get you back to
          the main event!
        </p>

        <!-- Action buttons -->
        <div class="space-y-4">
          <a
            href="/"
            class="inline-flex items-center px-8 py-4 bg-gradient-to-r from-blue-600 to-purple-600 text-white font-semibold rounded-2xl hover:from-blue-700 hover:to-purple-700 transition-all duration-300 transform hover:scale-105"
          >
            <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"
              ></path>
            </svg>
            Return to Home
          </a>

          <div class="text-sm text-gray-400">Or contact me directly if you need assistance</div>
        </div>

        <!-- Decorative elements -->
        <div class="mt-12 flex justify-center space-x-2">
          <div class="w-2 h-2 bg-blue-400 rounded-full animate-pulse"></div>
          <div class="w-2 h-2 bg-purple-400 rounded-full animate-pulse delay-75"></div>
          <div class="w-2 h-2 bg-pink-400 rounded-full animate-pulse delay-150"></div>
        </div>
      </div>

      <!-- Background decorative elements -->
      <div
        class="absolute top-1/4 right-1/4 w-64 h-64 bg-gradient-to-r from-blue-500/10 to-purple-500/10 rounded-full blur-3xl"
      >
      </div>
      <div
        class="absolute bottom-1/4 left-1/4 w-80 h-80 bg-gradient-to-r from-purple-500/10 to-pink-500/10 rounded-full blur-3xl"
      >
      </div>
    </div>

    <script>
      // Log 404 error for analytics/debugging
      console.error(
        "404 Error: User attempted to access non-existent route:",
        window.location.pathname
      );

      // Optional: Send 404 analytics event
      // if (typeof gtag !== 'undefined') {
      //   gtag('event', 'page_not_found', {
      //     page_path: window.location.pathname
      //   });
      // }
    </script>
  </body>
</html>
